import type { JSX } from 'react';
import { InitLayout } from '@features/init/components/init-layout';

const HomePage = ({}: HomePageProps): JSX.Element => {
  return <InitLayout />;
};

export default HomePage;
